window.onload = function () {
	var slideIndex = 1;
	var mySlideModule = (function () {
		var width = 800;
		var speedAnimation = 1000;
		var pause = 3000;

		var array = document.querySelectorAll('.slider img');
		var image = document.querySelectorAll('.slider-mini img');
		var interval;

		function slideShow(n) {
			slideIndex = n;
			
		}

	})
}